// Sets the nickname for the bot in the server
exports.run = (client, msg) => {
  // if (client.args[0].startsWith("<@") && !client.args[0].startsWith("<@&")) {
  //   msg.guild.members.get(client.args[0]).setNickname(client.args.shift().join(" "));
  //   msg.channel.send("Nickname changed to " + client.args.join(" "));
  // } else {
  msg.guild.member(client.user).setNickname(client.args.join(" "));
  msg.channel.send("Nickname changed to " + client.args.join(" "));
  // }
}

// CMD Help Info
exports.help = {
  "name": "Nick",
  "desc": "Changes the nickname of the bot to the provided value",
  "use": "nick [nickname]"
}